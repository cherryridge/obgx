

const trackedHosts :{name :string; id :string}[] = [
    { name: "CherryGrove", id: "cherrygrove" }
] as const;

export default function Compatable() {
    return (
        <div>
            
        </div>
    );
}