---
type: enum
---

This enum is used for the return type of [`getRecord`](getRecord).

```obgx
enum GetRecordResult {
    // !link[/"record"/] hndl_record
    Success(hndl("record")),
    NotFound
}
```

<CompatTable element="GetRecordResult" support={{
    cherrygrove_js: {
        status: "supported",
        versionAdded: "1"
    },
    cherrygrove_wasm: {
        status: "supported",
        versionAdded: "1"
    },
    cherrygrove_lua: {
        status: "unsupported"
    }
}}/>