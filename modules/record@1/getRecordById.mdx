---
type: function
---

This funtion returns the handle of an existing record type, given its ID, or returns `success: false` if the record type does not exist.

```obgx
// !link[/GetRecordByIdResult/] GetRecordByIdResult
getRecordById(recordId: string): GetRecordByIdResult;
```