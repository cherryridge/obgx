---
type: type
---

This type stores the metadata of a record and is used as parameter for the [`registerRecord`](registerRecord) function.

```obgx
type RecordDesc {
    recordId: id;
    // !link[/FieldDesc/] FieldDesc
    fields: FieldDesc[];
}
```