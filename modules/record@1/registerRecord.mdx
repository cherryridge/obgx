import Compatable from "@site/src/components/Compatable";

# `registerRecord`

## Summary

This function registers a new record to the host.

## Syntax

`registerRecord(record: RecordDesc): bool | RegisterRecordResult`

## Description



## Examples



## Compatibility

<Compatable element="registerRecord" support={{
    cherrygrove: {
        status: "supported",
        versionAdded: "1",
        versionRemoved: "3",
        notes: "The `registerRecord` function is fully supported in CherryGrove."
    }
}}
subfeatures={[
    {
        name: "Returns `RegisterRecordResult`",
        support: {
            cherrygrove: {
                status: "unsupported",
                notes: "The `registerRecord` function returns `bool` instead of `RegisterRecordResult` in CherryGrove."
            }
        }
    }
]}
/>

## Specifications


## See Also