---
type: function
---

This function returns the handle of an existing record type, given its name and the namespace of the caller pack.

```obgx
// !link[/GetRecordResult/] GetRecordResult
getRecord(name: string): GetRecordResult;
```