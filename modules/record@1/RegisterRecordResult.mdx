---
type: type
---

This type is used as the return type of the [`registerRecord`](registerRecord) function.

```obgx
type RegisterRecordResult {
    success: boolean;
    message: string;
    handle: hndl(record);
};
```