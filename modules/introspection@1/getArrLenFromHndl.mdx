---
type: function
---

This function returns the count of elements in an array, given its opaque handle.

```obgx
// !link[/"array"/] ../basic-handles@1/hndl_array
getArrLenFromHndl(arr: hndl("array")): u64
```