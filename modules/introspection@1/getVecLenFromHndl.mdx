---
type: function
---
This function returns the count of elements in a vector, given its opaque handle.

```obgx
// !link[/"vector"/] ../basic-handles@1/hndl_vector
getVecLenFromHndl(vec: hndl("vector")): u64;
```